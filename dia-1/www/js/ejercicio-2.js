/* #################
 * ## Ejercicio 2 ##
 * #################
 *
 * Dado el valor "num_a = 8" y "num_b = 2":
 *  - Muestra por consola la suma de A + B.
 *  - Muestra el resto de dividir A entre B.
 *  - Muestra el resultado de elevar A a B.
*/

'use strict';

// Declaramos dos constantes que almacenan un valor numérico.
const num_a = 8;
const num_b = 2;

// Mostramos por consola el resultado de la suma, el resto de dividir A entre B,
// y el resultado de elevar A a la potencia de B.
console.log(num_a + num_b, num_a % num_b, num_a ** num_b); 